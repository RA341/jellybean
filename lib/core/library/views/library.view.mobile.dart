import 'package:flutter/widgets.dart';

class LibraryMobileView extends StatelessWidget {
  const LibraryMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
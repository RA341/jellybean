import 'package:flutter/widgets.dart';

class PlayerMobileView extends StatelessWidget {
  const PlayerMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}